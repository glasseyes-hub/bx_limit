mixin panel(parentClass, title, link)
    - className = parentClass ? parentClass + "__panel" : '';

    section.panel(class = className)
        .panel-wrap
            if title && link
                .panel-header
                    h1.panel-title #{title}
                    a(href=link).panel-all Посмотреть все
            block
